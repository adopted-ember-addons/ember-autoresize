<textarea id={{inputId}}
  name={{name}}
  rows={{rows}}
  autocomplete="off"
  onpaste={{action 'setValue'}}
  onchange={{action 'setValue'}}
  oninput={{action 'setValue'}}
  disabled={{disabled}}></textarea>